<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.raspberry.raspberrypilager">
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21"/>


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menu.Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".uberuns.Uberuns" android:parentActivityName=".menu.Menu" android:label="Über uns"/>
        <activity android:name=".lieferung.Lieferung" android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".bestellung.Bestellung" android:parentActivityName=".menu.Menu" android:label="Bestellung"/>
        <activity android:name=".bestaetigen.Bestaetigen" android:parentActivityName=".bestellung.Bestellung" android:label="Warenkorb"></activity>
    </application>
</manifest>
